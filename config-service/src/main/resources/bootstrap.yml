server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/camano/Cuentas-java.git
          searchPaths: config-data
          default-label : master

